[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "bash start_railway.sh"
# No HTTP healthcheck — the bot is a worker process, not a web server.
# Railway will monitor the process directly.

# Restart the service if the process crashes
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
