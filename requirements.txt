# Astronomical Calculations
pyswisseph>=2.10.3.2
astropy>=6.0.0
ephem>=4.1.5

# Market Data & Trading
ccxt>=4.3.0
pandas>=2.2.0
numpy>=1.26.0

# Scheduling & Infrastructure
apscheduler>=3.10.4
python-dotenv>=1.0.0
loguru>=0.7.2
rich>=13.7.0

# PostgreSQL (Railway shared database)
psycopg2-binary>=2.9.9

# Dashboard API (Railway — only needed if running dashboard_api.py alongside the bot)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# Backtest plotting (optional — not needed in production)
# matplotlib>=3.9.0
# streamlit>=1.32.0
# plotly>=5.20.0
